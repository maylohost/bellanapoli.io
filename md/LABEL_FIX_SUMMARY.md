# Form label note (archived)

An older version of this file described a label swap that **does not match** the current `AdminPanel.tsx` implementation.

**Source of truth**

- Field mapping: [DATE_FIELD_MAPPING.md](./DATE_FIELD_MAPPING.md)  
- UI wiring: `components/AdminPanel.tsx` — `closing_date` uses **Data Chiusura Scommesse**, `closing_bid` uses **Data Chiusura Prediction**  

Ignore any historical “before / after” label tables that contradicted the above.
